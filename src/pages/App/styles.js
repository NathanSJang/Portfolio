import { makeStyles } from '@material-ui/core';


export default makeStyles((theme) => ({
  app: {
    maxWidth: '100%',
    paddingLeft: 0,
    paddingRight: 0,

  },
  mainContainer: {
    marginTop: '12vmin',
    height: '100vh',
  }
}));